# Swaminathan-Task5
Used Heart Disease data to train and compare Decision Tree and Random Forest classifiers. Visualized the tree, checked for overfitting, evaluated with cross-validation, and analyzed feature importance. Random Forest outperformed the Decision Tree.
